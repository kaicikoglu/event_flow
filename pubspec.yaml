name: event_flow
description: "A new Flutter project."

publish_to: 'none'

version: 1.0.0+1

environment:
  flutter: '3.22.2'
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  go_router: ^14.2.0

  flutter_riverpod: ^2.5.1

  isar: ^3.1.0
  isar_flutter_libs: ^3.1.0
  path_provider: ^2.0.0

  cupertino_icons: ^1.0.6

  intl: ^0.19.0

  image_picker: ^0.8.7

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0

  isar_generator: ^3.1.0
  build_runner: any

  analyzer: ^5.13.0

  flutter_launcher_icons: ^0.13.1

flutter:
  uses-material-design: true

flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/icon/event_flow.png"
